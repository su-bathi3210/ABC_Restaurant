import React from 'react';
import QueryComponent from '../Query/Query';

const CustomerQuery = () => {
    return (
        <div>
            <QueryComponent />
        </div>
    );
}

export default CustomerQuery;
